import SwiftUI

struct PickingFinishView: View {
    var body: some View {
        Button("Finish") {
            finish()
        }
            .navigationBarBackButtonHidden()
    }
    
    private func finish() {
        
    }
}
